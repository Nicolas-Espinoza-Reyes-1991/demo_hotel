# Requiere .env.production (generar: sh scripts/bootstrap-production-env.sh --force)
services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    env_file:
      - .env.production
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_BASE_PATH: /reservas
        # Dominio público de la landing (link "Volver al sitio web")
        NEXT_PUBLIC_WEBSITE_URL: http://lacasonadefutrono.cl
        # Usar http mientras HTTPS/Cloudflare no esté estable.
        NEXT_PUBLIC_APP_URL: http://lacasonadefutrono.cl
        NEXT_PUBLIC_SHOW_DEMO_UI: "false"
        NEXT_PUBLIC_DISPLAY_CURRENCY: CLP
        NEXT_PUBLIC_WHATSAPP_NUMBER: "56900000000"
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
